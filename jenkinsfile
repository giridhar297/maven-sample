pipeline{
  agent any
  def mvnHome
   stage('Preparation') {
      git url: 'https://github.com/giridhar297/maven-sample.git', branch: 'master'
      mvnHome = tool 'Maven'
   }
   stage('Build') {
      sh "'${mvnHome}/bin/mvn' -Dmaven.test.failure.ignore clean package"
   }
}